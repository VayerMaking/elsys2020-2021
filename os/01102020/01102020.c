#include<stdio.h>
size_t strlen( const char * );
size_t strlen(const char * input){
  int size;
  for(int i = 0;i<size_of(input);i++){
    size = i;
  }
  return size;
}

int main(){
  printf("%d\n", size_t strlen("asdf"));
  return 0;
}
